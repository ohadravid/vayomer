[project]
name = "data-proc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"

requires-python = ">=3.12"
dependencies = [
    "click>=8.1.0",
    "lxml>=5.3.0",
    "ollama>=0.6.1",
    "pytest>=8.4.2",
    "tqdm>=4.66.0",
]

[project.scripts]
data-proc = "data_proc:main"

[build-system]
requires = ["uv_build>=0.8.5,<0.9.0"]
build-backend = "uv_build"

