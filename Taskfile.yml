version: '3'

tasks:
  web:build:
    desc: Build web bundle (Bun)
    cmds:
      - test -d node_modules || bun install
      - bun run build

  web:serve:dist:
    desc: Build and serve dist (PORT defaults to 4173)
    cmds:
      - test -d node_modules || bun install
      - bun run build
      - sh -c 'cd dist && python3 -m http.server ${PORT:-4173}'

  web:test:
    desc: Run frontend unit tests
    cmds:
      - test -d node_modules || bun install
      - bun run test:web

  web:check:
    desc: Typecheck + test + build site
    cmds:
      - test -d node_modules || bun install
      - bun run check:web

  web:dev:
    desc: Dev server with watch + autoreload (Bun)
    cmds:
      - test -d node_modules || bun install
      - bun run dev

  web:setup:
    desc: Install web dependencies
    cmds:
      - bun install
